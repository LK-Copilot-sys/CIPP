services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "80"

  web-proxy:
    image: nginx:alpine
    depends_on:
      - web
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    expose:
      - "80"
    labels:
      - coolify.managed=true
